# Discover UI layout

| Folder | Role |
|--------|------|
| `components/discover/` | Hub, menus, gates, preview banner, park sections |
| `components/explore/` | Species cards, pager, category list, detail modal |

The Discover tab imports from both: hub chrome lives here; species browsing UI lives under `explore/` until a future folder merge.
