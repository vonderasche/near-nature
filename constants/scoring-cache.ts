/** Device cache key prefix (legacy AsyncStorage; native uses SQLite). */
export const SCORING_SNAPSHOT_CACHE_KEY_PREFIX = 'near_nature:scoring_snapshot:';

export const SCORING_SNAPSHOT_CACHE_VERSION = 3;
